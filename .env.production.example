# Production Environment Configuration
# Copy this file to .env in your production deployment directory
# Example: /var/www/tempmail/.env

# Server Configuration
NODE_ENV=production
PORT=5001
BASE_PATH=/tempmail

# Security: Generate with: openssl rand -hex 32
INBOUND_SHARED_SECRET=your_generated_secret_here_replace_this

# SendGrid SMTP Configuration
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SMTP_PASS=your_sendgrid_api_key_here

# Optional: Custom SendGrid sender email (defaults to noreply@redweyne.com)
# SMTP_FROM=noreply@yourdomain.com

# Optional: Database (defaults to SQLite in same directory)
# DATABASE_URL=file:./redweyne.db
